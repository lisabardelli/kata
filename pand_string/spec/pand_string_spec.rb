require "pand_string"

describe "pand_string" do
    it "returns a single letter "do
        expect(pand_string("l")).to eq true
    end
    it "returns teh revese string"do
    expect(pand_string("level")).to eq true
end
end